"""Octomate operator CLI (`octomate ...`).

The app is assembled in `octomate.cli.base`; each agent tentacle contributes its own
command group from its `typer` module. No logic lives in this package's dunder files.
"""
