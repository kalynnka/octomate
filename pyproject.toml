[project]
name = "octomate"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anyio>=4",
    "pydantic>=2.12.5",
    "pydantic-ai>=0.2",
    "pydantic-settings>=2",
    "pyyaml>=6",
    "websockets>=15",
]

[dependency-groups]
dev = [
    "watchfiles>=1.1.1",
]
